# Utilities

Shared Python helpers and project configuration live here.

- `config.py` defines the asset universe, date ranges, model list, labels, colors, and portfolio bounds.
- `utils.py` contains reusable data, portfolio, and diagnostics helpers.

Keep reusable project code in this folder instead of duplicating helper logic across numbered stages. The repository root should stay limited to documentation, requirements, and numbered project folders.
