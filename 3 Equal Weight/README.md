# 3 Equal Weight

This stage builds the equal-weight benchmark portfolio.

The benchmark provides a transparent reference for the active kernel portfolios. It should use the same bank universe, active dates, valuation calendar, and tradability conventions as the kernel strategies.

Use `output` for benchmark returns and weights. Use `img` for benchmark performance figures.
