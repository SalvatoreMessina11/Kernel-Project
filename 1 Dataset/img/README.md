# Dataset Figures

This folder stores dataset diagnostics generated by `1 Dataset/dataset.py`.

## Files

| File | What it is | What it shows |
| --- | --- | --- |
| `asset_price_tradable_bands.png` | Dataset coverage figure | Normalized asset-price paths with tradability bands, used to visually check whether the bank universe has usable coverage over the sample. |
| `README.md` | Folder documentation | Describes the dataset figure assets. |

## Usage

The final report copies or references this diagnostic through `7 Main/img`. Keep the filename stable if `main.tex` refers to it.
