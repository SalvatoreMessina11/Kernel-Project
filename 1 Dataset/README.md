# 1 Dataset

This stage builds the bank-level market dataset used by the rest of the project.

Expected work includes downloading bank prices, selecting adjusted closes when available, converting non-USD listings into USD, computing daily log returns, tracking tradability flags, and writing audit outputs for the active bank universe.

Use `intermediate output` for cleaned panels and audit files. Use `img` for dataset diagnostics and data-quality figures.
